import type { HookEvent, HookProvider, HookRegistry } from '../hooks/index.js';
/**
 * Mock hook provider that records all hook invocations for testing.
 */
export declare class MockHookProvider implements HookProvider {
    invocations: HookEvent[];
    private includeModelEvents;
    constructor(options?: {
        includeModelEvents?: boolean;
    });
    registerCallbacks(registry: HookRegistry): void;
    reset(): void;
}
//# sourceMappingURL=mock-hook-provider.d.ts.map